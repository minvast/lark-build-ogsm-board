# 扣子适配

将 `adapters/generic/system-prompt.md` 放入智能体系统提示词，把五个统一操作实现为插件能力或工作流，并保持操作名称和输入输出语义一致。

对话节点只负责收集参数、展示预检和获取明确确认；工作流负责规范化、完整分页、人员解析、确定性 upsert 和验收。若当前环境无法安全保存来源指纹、计划 ID 和写前快照，把能力降级为“预检与计划”，不要开放写操作。

飞书凭据、权限范围和接口调用方式由实际部署环境配置；不要把令牌或人员敏感信息写入提示词。
